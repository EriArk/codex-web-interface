# Optional Remote credential only. Never commit the real value.
REMOTE_MAIN_WINDOWS_PASSWORD=
# Website passwords are chosen through first-run enrollment, not this file.
# HUB_CONFIG=/absolute/path/config.yaml
# HUB_WEB_ROOT=/absolute/path/apps/web/dist
